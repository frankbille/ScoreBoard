<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dk.frankbille.scoreboard.dao.mybatis.UserMapper">
	<insert id="insertUser" parameterType="dk.frankbille.scoreboard.domain.User">
		INSERT INTO
			user
		(
			username,
			password
		)
		VALUES
		(
			#{username},
			#{password}
		)
	</insert>

	<select id="getUserWithUsername" resultType="dk.frankbille.scoreboard.domain.User">
		SELECT
			username,
			password
		FROM
			user
		WHERE
			username = #{username}
	</select>

	<select id="authenticate" resultType="dk.frankbille.scoreboard.domain.User">
		SELECT
			username,
			password
		FROM
			user
		WHERE
			username = #{username}
			AND
			password = #{password}
	</select>
</mapper>
